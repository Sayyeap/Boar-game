{
    "name": "boar-game",
    "version": "1.0.0",
    "description": "A Phaser game with boars and upgrades",
    "scripts": {
        "start": "live-server --port=8080",
        "build": "if exist dist rmdir /s /q dist && mkdir dist && copy index.html dist\\index.html && xcopy src dist\\src /E /I /Y && xcopy assets dist\\assets /E /I /Y"
    },
    "dependencies": {
        "live-server": "^1.2.2",
        "phaser": "^3.55.2"
    }
}